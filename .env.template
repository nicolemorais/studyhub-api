# ------------------------------------------------------------
# COMANDO WINDOWS PARA LER O ARQUIVO .ENV NO TERMINAL:
# Get-Content .env | Where-Object { $_ -and -not $_.StartsWith('#') } | ForEach-Object { $name, $value = $_.Split('=', 2); [System.Environment]::SetEnvironmentVariable($name.Trim(), $value.Trim(), "Process") }
# ------------------------------------------------------------

# ==========================================
# MICROSSERVIÇO: AUTH SERVICE
# ==========================================
# Conexão física isolada para o banco de autenticação
AUTH_DB_URL=jdbc:postgresql://localhost:5432/studyhub_auth_db
AUTH_DB_USER=auth_app_runtime
AUTH_DB_PASSWORD=SenhaDoMicrosservico123

# Segurança e Criptografia
JWT_SECRET=coloque_aqui_uma_chave_aleatoria

# ==========================================
# FUTUROS MICROSSERVIÇOS (Apenas como placeholder)
# ==========================================
# CLASSROOM_DB_URL=jdbc:postgresql://localhost:5432/studyhub_classroom_db
# CLASSROOM_DB_USER=classroom_app_runtime
# CLASSROOM_DB_PASSWORD=...